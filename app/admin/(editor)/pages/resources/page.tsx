import styles from '../../../admin.module.css';

export default function AdminResourcesPage() {
  return (
    <div className={styles.placeholder}>
      <p className={styles.eyebrow}>Next phase</p>
      <h1 className={styles.h1}>Resources page editor</h1>
      <p className={styles.p}>This editor will follow once the first editable sections are confirmed and working in production.</p>
    </div>
  );
}
