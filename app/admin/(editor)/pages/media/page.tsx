import styles from '../../../admin.module.css';

export default function AdminMediaPage() {
  return (
    <div className={styles.placeholder}>
      <p className={styles.eyebrow}>Next phase</p>
      <h1 className={styles.h1}>Media page editor</h1>
      <p className={styles.p}>The media editor will manage video titles, embed URLs, and descriptions in a later pass.</p>
    </div>
  );
}
